# Workspace Agent Instructions

- Always in the Prompt window show `@cloudflare` option with `Act Mode`.
- Always in the Prompt window show `@netlify` option with `Act Mode` for Netlify tasks.
- Always in the Prompt window show `@slack` option with `Act Mode` for Slack tasks.
- Always in the Prompt window show `@build-web-apps` option with `Act Mode` for web app tasks.

